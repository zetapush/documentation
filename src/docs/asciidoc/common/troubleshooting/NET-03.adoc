:relative-path: ../../
include::{docdir}/variables.adoc[]

Your computer can access local network but can't access the proxy.

++++
include::{docdir}/common/troubleshooting/schemas/NET-03-cause.html[]
++++

////
TODO: test access to the proxy
TODO: check if NodeJS configured with a proxy and have access to Internet through the proxy
TODO: check if Internet not accessible because of corporate proxy not configured
////