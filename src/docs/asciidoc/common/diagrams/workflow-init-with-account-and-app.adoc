:relative-path: ../../
include::{docdir}/variables.adoc[]

[plantuml, init-with-account-and-app, png, role="center"]     
....
start

:I launch :
<color:#38363a>$ npm init @zetapush myApp -l user@gmail.com -p password -a myApp ➊;

:An application with an example is created ➋;

:<color:#38363a>.zetarc</color> file is created ➌;

:I launch :
<color:#38363a>$ zeta run</color> or
<color:#38363a>$ zeta push;

:<b>Deployment success on the existing application;
....
➊ I launch `npm init @zetapush myApp` with the `--developer-login`, `--developer-password` and `--appName` parameters +
➋ The application is created in `myApp` folder +
➌ The _.zetarc_ file is like :
[source, json, linenums]
----
{
    "developerLogin": "user@gmail.com",
    "developerPassword": "password",
    "appName": "nameOfAppAutoGenerated"
}
----
