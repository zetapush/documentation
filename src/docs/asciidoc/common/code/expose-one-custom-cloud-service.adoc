:relative-path: ../../
include::{docdir}/variables.adoc[]

.worker/index.js
[source, javascript, linenums, role="highlight-lines:24"]
----
const { Inject } = require('@zetapush/platform');

class Calendar { <1>
  getNow() {
    return new Date().toLocaleDateString();
  }
}

class MyPublicCustomCloudService { <2>
  static get parameters() {
    return [
      new Inject(Calendar)
    ];
  }
  constructor(calendar) {
    this.calendar = calendar;
  }
  
  async hello() {
    return `Hello World at ${this.calendar.getNow()}`;
  }
}

module.exports = MyPublicCustomCloudService; <3>
----
<1> `Calendar` is my private _custom cloud service_
<2> `MyPublicCustomCloudService` is my exposed _custom cloud service_
<3> We expose only `MyPublicCustomCloudService` to the client side
